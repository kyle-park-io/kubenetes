---
apiVersion: v1
kind: Service
metadata:
  name: blogserv
  labels:
    app: blogserv
spec:
  type: ClusterIP
  selector:
    app: blogDep
  ports:
    - port: 8080
      targetPort: 8080
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: dex
#   labels:
#     app: dex
# spec:
#   type: ClusterIP
#   selector:
#     app: dexDep
#   ports:
#     - port: 8080
#       targetPort: 8080
